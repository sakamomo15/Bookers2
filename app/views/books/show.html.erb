<% if @book_detail.errors.any? %>
  <%= @book_detail.errors.count %>error prohibited this obj from being saved:
  <ul>
    <% @book_detail.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'users/userinfo', user: @user %>

<%= render 'newbook', book: @book %>

<h1>Book detail</h1>

<div>
  <%= image_tag @user.get_profile_image(100,100) %>
  <%= link_to "#{@user.name}", user_path(@user.id) %>
</div>
<%= link_to "#{@book_detail.title}", book_path(@book_detail.id) %>
<%= @book_detail.opinion %>

<% if @book_detail.user == current_user %>
  <%= link_to "Edit", edit_book_path(@book_detail) %> <!--(@book_detail.id)では？-->
  <%= link_to "Destroy", book_path(@book_detail.id), method: :delete, "data-confirm" => "本当に消しますか？" %><!--book_path(@book_detail.id)では？？-->
    <!--URLに対して削除-->
<% end %>
